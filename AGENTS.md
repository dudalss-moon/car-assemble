# AGENTS.md

이 브랜치는 `car-assemble` 프로젝트의 **리팩토링** 작업 브랜치입니다.

## 프로젝트 개요

- **언어**: Java
- **빌드 도구**: Gradle
- **테스트 프레임워크**: JUnit Jupiter (JUnit 6)
- **진입점**: `src/main/java/Assemble.java`

## 현재 코드 상태

모든 로직이 `Assemble.java` 단일 클래스에 static 메서드로 구현되어 있습니다.

- 차량 타입 / 엔진 / 제동장치 / 조향장치 선택을 int 상수로 관리
- UI(메뉴 출력), 비즈니스 로직(유효성 검사, 동작), 상태(stack 배열)가 혼재
- 부품 조합 제약 조건이 `isValidCheck()` / `testProducedCar()` 두 곳에 중복 존재

## 리팩토링 목표

1. **관심사 분리** — 메뉴 출력(View), 입력 처리(Controller), 조립 로직(Domain) 계층 분리
2. **상수 → Enum** — int 상수(`SEDAN`, `GM` 등)를 Enum 타입으로 교체
3. **중복 제거** — `isValidCheck()`와 `testProducedCar()`의 제약 조건 중복 통합
4. **테스트 가능한 구조** — 비즈니스 로직을 순수 메서드로 분리하여 JUnit 테스트 작성 가능하게

## 클린 코드 원칙

> 이 프로젝트의 모든 리팩토링은 아래 원칙을 기준으로 작성합니다.

### 가독성 우선
- 읽기 쉬운 코드가 좋은 코드입니다. 코드는 글처럼 위에서 아래로 자연스럽게 읽혀야 합니다.
- 이해하기 쉬운 코드는 곧 유지보수가 좋은 코드입니다. 동료가 설명 없이 읽을 수 있는지를 기준으로 삼습니다.

### 이름에는 의미가 있어야 한다
- 변수, 메서드, 클래스 이름만으로 역할을 파악할 수 있어야 합니다.
- 축약어, 번호, 모호한 단어(`data`, `info`, `tmp` 등)는 사용하지 않습니다.

  ```java
  // Bad
  int[] stack = new int[5];

  // Good
  CarParts selectedParts = new CarParts();
  ```

### 함수는 한 가지 역할만 담당한다
- 하나의 메서드는 하나의 일만 합니다. 이름으로 설명되지 않는 부작용은 만들지 않습니다.
- 메서드가 길어진다면 역할이 두 개 이상인 신호입니다. 분리를 검토합니다.

### 클래스는 하나의 책임을 갖는다 (SRP)
- 클래스는 변경되어야 하는 이유가 하나여야 합니다.
- 현재 `Assemble.java`는 UI 출력 / 입력 처리 / 유효성 검사 / 상태 관리를 모두 담당하고 있어 SRP를 위반합니다.

  | 책임 | 담당 클래스 |
  |------|------------|
  | 메뉴 출력 | `ConsoleView` |
  | 입력 파싱 / 흐름 제어 | `InputHandler` |
  | 부품 조합 유효성 검사 | `CarValidator` |
  | 조립 상태 보관 | `Car` |

### 중복은 제거한다
- 동일한 로직이 두 곳 이상에 존재하면 반드시 하나로 통합합니다.
- 현재 `isValidCheck()`와 `testProducedCar()`의 제약 조건 중복이 대표적인 사례입니다.

### 테스트 코드는 필수다
- **테스트는 리팩토링 전에 먼저 작성합니다.** 현재 동작을 테스트로 고정한 뒤 구조를 변경합니다.
- 테스트가 GREEN인 상태에서 리팩토링을 시작하고, 리팩토링 후에도 GREEN을 유지해야 합니다.
- 테스트는 리팩토링이 기존 동작을 깨뜨리지 않았음을 보장하는 유일한 수단입니다.

---

## 작업 규칙

- **테스트 먼저(Test-First)**: 리팩토링 전 현재 동작을 테스트 코드로 먼저 고정합니다.
- 기존 동작(입출력 메시지, 제약 조건)을 변경하지 않고 구조만 개선합니다.
- 새 클래스는 `src/main/java/` 아래에 패키지 단위로 구성합니다.
- 리팩토링 전후 반드시 테스트가 통과하는지 확인합니다. (`src/test/java/`)
- 빌드 및 테스트는 아래 명령으로 확인합니다.

```bash
./gradlew build
./gradlew test
```
